import 'package:flutter/material.dart';

class ActionConfirmationPage extends StatelessWidget {
  const ActionConfirmationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
